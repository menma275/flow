specVersion: "v1beta"
serviceId: "flow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
